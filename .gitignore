coverage*
node_modules
npm-debug.log*
.DS_Store
release-notes.md

# Design specs / plans are local-only working notes, not part of the package.
docs/superpowers

# Build output — shipped in the npm tarball (via package.json "files") but
# rebuilt fresh by CI/release ("npm run compile"), so it is not committed to git.
dist
esm
*.tgz
